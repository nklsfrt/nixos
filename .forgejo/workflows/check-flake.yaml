on: [push]

jobs:
  check_flake:
    runs-on: native
    steps:
      - name: check out repo code
        uses: actions/checkout@v4
      
      - name: verify WD
        run: ls .

      - name: check flake
        run: nix flake check